#include <iostream>
#include <bits/stdc++.h>

// Constants
#define PI 3.14
#define E 2.71815

// Functions
#define max(x, y) (x > y ? x : y)
#define min(x, y) (x < y ? x : y)


using namespace std;


int main(int argc, char const *argv[])
{
    int r = 10;
    cout << r * r * PI << endl;;

    cout << E * E << endl;
    
    return 0;
}
